# Strapi CMS Configuration
STRAPI_URL=http://localhost:1337
STRAPI_TOKEN_RO=your-read-only-api-token

# Site URL for SEO and canonical URLs
SITE_URL=https://coinerd.github.io/zeiler_me_pages

# Base path for GitHub Pages subdirectory deployment
# For root deployment (custom domain): leave empty or set to ""
# For subdirectory deployment: set to the repository name, e.g., "zeiler_me_pages"
BASE_PATH=zeiler_me_pages
